/* Reverses a series of numbers using a variable-length array in C99 */
#include<stdio.h>
int main(void)
{
	int i, n=0;
	char ch;



	printf("Enter a series of  numbers to be reversed, separated by a space: ",n);
	for(i=0;ch!='\n';i++)
	{
		scanf(" %d",&a[i]);
		ch=getchar();
	}
		
	int a[i];
	
	printf("In reverse order:");
	for (;i>=0;i--)
		printf(" %d", a[i]);
	printf("\n");

	return 0;
}

